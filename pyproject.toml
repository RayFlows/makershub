[project]
name = "makershub-v2-workspace"
version = "0.1.0"
description = "MakersHub V2 workspace"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["apps/api"]

[tool.ruff]
line-length = 100
target-version = "py310"
