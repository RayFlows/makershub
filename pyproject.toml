[project]
name = "makershub-workspace"
version = "0.1.0"
description = "MakersHub workspace"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["apps/api"]

[tool.ruff]
line-length = 100
target-version = "py310"
