# 后端测试支撑工具

本目录放置跨测试文件复用的测试支撑工具。

这里负责：

- 创建基于 `tmp_path` 的临时 SQLite 数据库；
- 创建带测试数据库依赖覆盖的 `TestClient`；
- 提供开发态微信登录、Bearer 请求头和测试直授角色 helper；
- 为后续业务测试逐步减少重复样板代码。

这里不负责：

- 隐藏业务断言；
- 改写业务状态流转；
- 生成复杂 mock 数据；
- 替代各业务测试自己的 seed 函数。

测试直授角色只允许用于自动化测试准备数据，不能成为生产代码路径。
