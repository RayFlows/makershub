# minio

MinIO 对象存储适配目录，负责创建 S3 兼容客户端和执行对象存储健康检查。

本目录负责：

- 根据运行时配置创建 MinIO 客户端；
- 在 readiness 中检查对象存储是否可用；
- 隔离具体 SDK 细节，避免业务模块直接绑定 MinIO 实现。

本目录不负责：

- 文件上传接口；
- 文件元数据登记；
- 业务文件权限；
- 对象生命周期归档。

业务文件的 bucket、object key、用途和 hash 等信息应通过 `modules/files` 登记，不能只依赖对象存储中的裸文件。
