# audit

审计 V1 接口目录，负责提供后台和运维查看审计日志的 HTTP 入口。

本目录负责：

- 按权限点 `system.audit.view` 限制审计日志读取；
- 将审计模型转换为接口响应；
- 保持审计读取接口与业务写入逻辑分离。

本目录不负责：

- 创建审计日志；
- 修改或删除审计日志；
- 替代运行日志。

审计日志默认只追加。业务服务在关键操作完成时调用审计服务写入记录。
