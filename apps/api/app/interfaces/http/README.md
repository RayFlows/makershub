# http

HTTP 接口适配目录，负责 FastAPI 路由、依赖注入、安全声明、请求模型和响应模型。

本目录负责：

- 声明 HTTP 路由；
- 解析当前用户和权限依赖；
- 将服务层结果转换为统一响应；
- 给 Swagger/OpenAPI 提供清晰的接口契约。

本目录不负责：

- 直接写数据库业务逻辑；
- 绕过服务层处理权限；
- 在接口层散落业务规则。

接口层应该尽量薄。复杂业务分支放到 `modules/<业务域>/service.py`，HTTP 层只做协议适配。
