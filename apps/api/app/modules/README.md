# modules

业务域模块目录。每个业务域应该拥有自己的模型、schema、仓储、服务、权限、路由和事件。

业务模块按业务边界拆分，不按协会部门拆分。

当前已开始落地的基础业务域：

- `identity`：用户主体、微信登录、本地账号、双 token 会话；
- `organization`：成员资料、部门、职务关系；
- `audit`：审计日志和重要操作追踪；
- `files`：文件元数据、对象 key 生成和文件状态管理。
