# 身份与登录

负责用户登录身份、本地账号、微信身份、会话、令牌和初始化超级管理员。

不负责成员部门、职务和业务权限授予。

## 身份链路约束

- 普通用户第一版以小程序微信无感登录为前置：先创建或找到用户主体，再在小程序个人主页绑定邮箱；
- 网页端首次邮箱验证码登录只接受已绑定到用户主体的邮箱，验证通过后强制设置密码；
- 第一版不支持普通用户先注册网页账号再绑定微信；
- 第一个 `999` 是例外，可以通过受控运维命令先创建本地账号，后续再绑定微信身份。

## 已落地的基础能力

- `users`：用户主体表；
- `local_accounts`：邮箱和密码账号表，允许存在“邮箱已绑定、密码未设置”的待激活本地账号；
- `wechat_accounts`：微信身份表；
- `email_verification_codes`：邮箱验证码记录表；
- 密码哈希和校验工具；
- 初始化唯一 `999` 超级管理员的服务；
- 微信登录创建或复用用户主体的服务；
- 微信用户绑定邮箱后生成待设置密码本地账号的服务；
- 访问令牌签发、当前用户解析、微信登录和 `/auth/me` HTTP 接口。

## 初始化 999

在 Docker 开发环境中执行：

```bash
docker compose -f infra/docker/compose.dev.yml exec api sh -lc \
  'INITIAL_SUPER_ADMIN_PASSWORD="<change-me>" uv run python -m app.cli bootstrap-super-admin --email admin@example.com'
```

该命令会创建一个用户主体、本地账号，并授予 `999` 职务。系统中已经存在有效 `999` 时，命令会拒绝继续创建。

生产环境不要把真实初始密码写入仓库或 shell 历史，应由部署平台 secret 或临时受控环境变量传入。

## 后续待实现

- 邮箱验证码发送、限流和消费；
- 首次邮箱登录和强制设置密码；
- 密码登录和访问令牌；
- 邮箱绑定、首次设置密码、邮箱密码登录和密码重置的 HTTP 接口。
