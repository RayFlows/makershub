# 职务能力

本目录负责协会普通职务读取和成员职务关系维护。

职务用于表达外部成员、干事、部长、副会长、会长、指导老师等协会身份。`998/999`
属于底层系统身份，不允许通过普通成员职务接口维护。

## 当前能力

- 列出后台可维护的普通协会职务；
- 替换成员当前普通协会职务；
- 支持全局职务和部门作用域职务；
- 拒绝通过成员职务接口维护 `998/999`。

## 不负责什么

- 不负责系统底层身份的初始化、恢复和指定；
- 不负责权限点判断和审计日志；
- 不负责部门主数据维护。
