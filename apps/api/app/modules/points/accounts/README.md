# 积分账户能力

本目录负责积分账户读取和懒创建。

积分账户是账本余额缓存，每个用户最多一条。账户创建前必须确认用户主体存在；余额变动
不在这里完成，而是由 `ledger`、`holds` 和 `adjustments` 能力追加流水后同步维护。

## 当前能力

- 按用户读取积分账户；
- 用户首次查看或发生积分操作时懒创建 0 积分账户；
- 确认积分账户归属用户存在。

## 不负责什么

- 不负责人工补发或扣减；
- 不负责冻结、解冻和冻结转扣除；
- 不负责权限判断和审计日志。
